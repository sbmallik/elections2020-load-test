name: 'elections2020-thorium-endpoints' ## --name argument
env: 'production' ## --env argument
scripts:
  'thorium-artillery.yaml': ## --script argument
    workers: 1 ## --workers argument
    rounds: 1 ## --rounds argument
secret:
  - TAAPI_API_KEY
